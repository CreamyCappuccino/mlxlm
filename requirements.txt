# MLX-LM Requirements

# Core runtime dependencies
mlx  # Apple MLX core framework
mlx-lm  # High-level language model utilities built on MLX
huggingface_hub  # Model metadata lookup and snapshot downloads

# CLI / terminal experience
prompt_toolkit>=3.0.43  # Interactive terminal UI (multiline, history, completion)

# Experiment utilities
matplotlib  # Optional: visualization helpers under trm_exp/

# Testing
pytest  # Unit test runner used in tests/
